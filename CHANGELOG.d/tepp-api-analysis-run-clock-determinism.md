`tepp_api::AnalysisRunRequest` now separates canonical wire serialization from live knowledge-cutoff admission. A syntactically valid RFC 3339 cutoff serializes deterministically without consulting the current wall clock, while `from_json` continues to reject a cutoff that is still in the future at live admission. This prevents immutable request evidence from changing serializability merely because verification occurs at a different time.
