### Scientific simulation

- Expose the known-topic prevalence EventTime basis directly from digest-bound latent event truth. Recovery consumers can now reconstruct the simulator's normalized prevalence predictor without copying the private generator formula; malformed one-event, ordinal, or non-affine clock geometry fails closed.
