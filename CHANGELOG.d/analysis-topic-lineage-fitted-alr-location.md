### Added

- `topic_measurement` now exposes `ReferenceTopicFit`, an owner-issued nominal aggregate that privately retains the exact admitted `ReferenceTopicInput`, deterministic `ReferenceTopicModelConfig`, and resulting `ReferenceTopicModel`. Its public `FittedDocumentCoordinateSummary::from_bound_fit` path pairs each retained document row's fitted ALR location with the model's existing positive diagonal-Laplace variance under the same numerator/reference topic indexes without accepting a detached input/model pair.
- This is fit-local numerical integrity, not Evidence authentication. The unreleased `tepp.trsl_topic_lineage.v2` wire is unchanged; release projection remains blocked on #664/#665 migration together with #663 fitted-topic basis binding and #658/#527 source/vocabulary provenance. The fit-local view does not claim joint covariance, independent plausible values, interval coverage, calibration, or semantic topic authority.
