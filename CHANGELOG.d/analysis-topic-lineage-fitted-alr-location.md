### Added

- `topic_measurement` now exposes a versioned fit-local document-coordinate view that pairs each supplied admitted document row's fitted ALR location with the CPU reference model's existing positive diagonal-Laplace variance under the same numerator/reference topic indexes. Analysis does not recompute curvature.
- `ReferenceTopicModel` does not yet carry an owner-issued binding to the exact `ReferenceTopicInput` that produced it, so this view validates numerical compatibility but is not fit/input provenance or release authority. #665 owns that integrity boundary. The unreleased `tepp.trsl_topic_lineage.v2` wire is unchanged; release projection remains blocked on #664/#665 together with #663 fitted-topic basis binding and #658/#527 source/vocabulary provenance. The fit-local view does not claim joint covariance, independent plausible values, interval coverage, calibration, or semantic topic authority.
