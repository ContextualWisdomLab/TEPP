- `GET /v1/interpretation-runs/by-run-id/{interpretation_run_id}/request` returns the accepted contextual-orchestrator stored create request on `tepp-orchestrator-loopback` (ADR 0097). Dual identity of stored-request GET (`idempotency_key`). Zero and ambiguous matches fail closed. `scientific_authority` remains false. Does not infer causality. Naruon and LineageWeave refused. `NaruonLiveService` stays POST-only. Does not re-open cancel lineages. Not GAP-010 Figma/export, not persistence.
