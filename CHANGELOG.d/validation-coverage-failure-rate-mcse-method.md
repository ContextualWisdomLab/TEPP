# Coverage-calibration failure-rate MCSE provenance

- Bind the unconditional numerical-failure-rate Monte Carlo standard-error rule to the versioned validation design as `tepp.coverage.failure_rate_mcse.bernoulli_plugin_sqrt_p_one_minus_p_over_n.v1`.
- Advance the coverage-design fingerprint domain to v7 and persist the reporting-method identity in schema-v9 calibration evidence beside `failure_rate_standard_error`.
- Keep the existing Bernoulli plug-in arithmetic `sqrt(p_hat * (1 - p_hat) / n)` over the unconditional attempted-DGP denominator. This change does not define an acceptable numerical-failure rate or promote a scientific/release claim. (#748)
