- `model_selection` now derives candidate `K` from the fitted topic dimension, scores only owner-issued `ReferenceTopicFit` aggregates, rejects zero/duplicate deterministic seed manifests before fitting, and names the existing in-sample `ℓ - (p ln N)/2` diagnostic as a Schwarz model-selection score instead of falsely presenting it as held-out likelihood. Actual leakage-safe held-out/rolling-origin validation remains separate scientific acceptance evidence.
