`CoverageCalibrationEvidenceRecord` schema v6 no longer serializes or exposes a single `supports_calibration_claim` boolean while numerical-failure acceptability is intentionally undefined. Consumers must inspect the prospective coverage-band and Monte Carlo precision decisions together with attempted/success/failure counts, failure rate, and failure-rate Monte Carlo standard error. This change does not define a failure-rate threshold or promote scientific acceptance.
