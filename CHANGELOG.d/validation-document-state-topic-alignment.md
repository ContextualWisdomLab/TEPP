## Validation: document-state topic alignment

- Added fail-closed projection of fitted document topic-probability vectors and row sets into the globally aligned known-truth topic order, preserving document row order while rejecting dimension, non-finite, negative, and non-normalized simplex geometry.
- The projection is validation-coordinate arithmetic only. Document identity binding remains with the fit-retained estimator input; semantic topic identity, provenance, release activation, and the #680 recovery acceptance decision remain separate owner responsibilities.