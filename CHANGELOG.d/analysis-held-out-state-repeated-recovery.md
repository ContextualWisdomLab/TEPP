# Repeated held-out state recovery

- Exercise #719/#720 held-out document-state recovery across the same four independent known-truth DGP seeds and five leakage-safe expanding rolling-origin windows used by the scientific recovery surface.
- Fit only the predeclared truth-K training model in each window, bind local-state inference to the exact admitted evaluation identities, and treat any owner-classified numerical fit/state failure as a failed DGP replication rather than truncating the temporal horizon.
- Align every recovered evaluation simplex through the global truth-topic assignment, join simulator truth by document UUID, compute held-out simplex RMSE and non-cancelling mean-absolute residual per window, then collapse windows within each DGP replication before Monte Carlo aggregation.
- Preserve the unconditional attempted DGP denominator and failure-rate Monte Carlo standard error through `summarize_recovery_metric_replications`; rolling-origin windows are not counted as independent Monte Carlo replications.

This remains CI-scale recovery evidence. It does not set a post-hoc practical acceptance threshold, calibrate intervals, or close #680.
