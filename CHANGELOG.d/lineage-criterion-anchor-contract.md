### Added

- Publish the strict `tepp.lineage_criterion_anchor.v1` artifact contract so
  LineageWeave can activate fast-mlsirm channel weights only after an exact,
  TEPP-authored independent criterion-validity result.
