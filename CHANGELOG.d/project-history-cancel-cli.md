- `tepp-project-history-cancel cancel` mints LineageWeave `POST /v1/project-histories/{idempotency_key}/cancel` onto spawned `tepp-loopback` TCP (ADR 0080). Metric-free `cancelled=true` receipts with `inference_status=temporal_association_only`. `tepp.scientific_acceptance.v1` never appears. Does not infer causality. Naruon refused. `NaruonLiveService` stays POST-only. Not project-history collection CLI, not export cancel CLI, not interpretation-run cancel CLI, not GAP-010 Figma/export, not persistence.
