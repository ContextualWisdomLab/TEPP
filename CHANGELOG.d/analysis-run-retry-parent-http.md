- `tepp_api` loopback `GET /v1/analysis-runs/{run_id}/parent` returns the metric-free parent identity of a listed run so operators can inspect which parent a retry child was cloned from (ADR 0038). Original runs return `"parent": null`. GET-by-id remains refused. Not lifecycle POST, not cancel, not collection GET, not retry POST, not stored-request GET, not retry-lineage GET, not idempotency-key lookup GET, not persistence.
