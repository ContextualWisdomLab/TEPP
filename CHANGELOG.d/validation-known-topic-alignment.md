- `validation_core` now aligns fitted topic-term probability rows to a known simulation truth basis with a deterministic global one-to-one minimum-cost assignment over squared Hellinger distance. Scientific recovery no longer relies on raw component indexes or greedy row matching. Fitted additive-log-ratio document/prevalence vectors can also be re-expressed against the aligned truth reference topic before recovery residuals are computed, rather than merely permuting `K-1` coordinates with the wrong denominator. These mappings are validation-local: they do not mutate fitted state, transform covariance matrices, mint semantic topic identity, authenticate Evidence vocabulary provenance, or create release authority.