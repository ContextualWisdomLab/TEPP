# Scientific validation

- Bind the frozen training prevalence coordinate system and the exact fitted reference state in a single owner-issued `ReferenceTopicTrainingFit`. Rolling-origin and held-out consumers can no longer preserve owner authority while pairing a dimension-compatible fit from one training state with the prevalence basis from another. This is numerical coordinate integrity only; Evidence, Membership, relation activation, and source/event-time provenance remain with their owning bounded contexts.
