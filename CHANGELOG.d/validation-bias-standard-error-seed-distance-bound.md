- Add a repository-owned binary64 seed-distance derivation for `correctly_rounded_scaled_sqrt_ratio`. Under its existing checked numerator/denominator/power-of-two admission contract, the floating seed starts fewer than three adjacent representable steps from the correctly rounded finite result, so at most two neighbor advances are needed before the next exact-square/midpoint decision.
- Keep the exact midpoint comparisons and terminal fail-closed refusal authoritative. The derivation is evidence for a later executable contract/source simplification, not permission to return an unchecked seed, weaken bounded termination, suppress the remaining coverage line, or claim the Rust coverage gate GREEN.