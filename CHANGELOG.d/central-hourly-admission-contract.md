- The hourly commercial-development quality contract now follows the protected-main centralized admission boundary: `cwl-org-commercial-entrypoint: v1` plus repository-local `workflow_dispatch`/`dry_run` remain required, while repository-local `schedule`/`cron` triggers are explicitly forbidden. The operations runbook now assigns recurring cadence and disablement to the central CWL scheduler/admission owner without changing TEPP workflow credentials, queue gates, verification, or late-publication controls.
