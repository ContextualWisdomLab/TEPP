- `tepp_api` loopback `GET /v1/analysis-runs/by-idempotency/{idempotency_key}` returns the metric-free identity of the unique run that used that key so operators can jump from a 202 receipt or retry child key without scanning collection pages (ADR 0037). GET-by-id remains refused. Not lifecycle POST, not cancel, not collection GET, not retry POST, not stored-request GET, not retry-lineage GET, not persistence.
