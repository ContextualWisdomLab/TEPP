### Fixed

- Bind scientific-recovery promotion to a domain-separated SHA-256 of the exact represented grouped truth/recovery payload, including profile identity, replication/coordinate order, and cardinality. Signed zero is canonicalized so `+0.0` and `-0.0` do not create distinct scientific evidence identities; execution attestation and pre-execution registration remain separate provenance requirements.
