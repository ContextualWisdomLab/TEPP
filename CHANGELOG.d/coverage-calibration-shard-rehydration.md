## Fixed

- Added strict owner-issued rehydration for persisted prospective coverage-calibration shards. The private JSON parser requires the current shard schema, rejects undeclared top-level and per-outcome fields, requires canonical validation/scenario fingerprints and a canonical lowercase source commit, a non-empty half-open ordinal range, and exact contiguous indexed outcomes before returning the typed application record. Public recovery is available only through `CoverageCalibrationShardRecord::from_json_with_sha256(...)`, which also verifies the persisted owner digest. Current-design/current-scenario equality, full 0..N tiling, scientific coverage-value validation, denominator arithmetic, and final schema-v4 evidence remain with their existing owners. The digest is an application-level transfer-integrity binding and does not authenticate external storage, runners, or builds.
