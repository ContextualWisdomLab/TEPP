## Fixed

- Added an owner-issued `CoverageCalibrationShardRecord::from_json(...)` recovery path for persisted prospective coverage-calibration shards. Rehydration now requires the current shard schema, rejects undeclared top-level and per-outcome fields, requires a canonical lowercase source commit and scenario fingerprint, a non-empty half-open ordinal range, and exact contiguous indexed outcomes before returning the typed application record. The parser reconstructs outcomes through `validation_core` constructors; current-scenario equality, full 0..N tiling, scientific coverage-value validation, denominator arithmetic, and final schema-v4 evidence remain with their existing owners. Round-trip recovery preserves the shard SHA-256 exactly and does not claim to authenticate external storage, runners, or builds.
