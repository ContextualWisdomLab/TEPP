- Add exact-proof budget characterization for mean-bias standard error: a seventeen-observation represented-input fixture, O(n²) versus algebraically equivalent O(n) checked-`u128` numerator evidence, and the compact-dyadic `n<=2_047` worst-case arithmetic envelope.
- Add a reproducible standard-library timing harness for the buffered O(n²), allocation-free two-pass O(n²), canonical-dyadic checked O(n), and O(n)→pair-fallback kernels. The initial landing kept the production exact route bounded at `n<=16` while resource evidence was collected; the current production source has since removed that staircase from the O(n) proof path: the checked neutral-zero proof is eligible for every `n>=3`, the O(n²) pairwise reference is used only after O(n) refusal at `n<=16`, and refusal above sixteen delegates directly to the established fallback without quadratic pair allocation.
- Add a dependency-free two-limb wider-product O(n) reference for characterization. On the canonical odd boundary `D=2^58+1,n=65`, the narrow checked-`u128` O(n) intermediates require 129 bits and refuse, while the exact pair numerator remains 123 bits; the wider-product reference performs the cancellation without overflow and recovers the same exact pair numerator. This is evidence about intermediate width, not a new arithmetic owner or a claim that every represented input admits the checked O(n) proof.
- Extend the timing harness to emit the wider-product reference beside the existing kernels so recorded Rust 1.98.0 release-mode runs can compare CPU cost without adding a production dependency or weakening fail-closed fallback. Resource measurements remain geometry/environment specific and do not substitute for applicable buyer-path p95 evidence.