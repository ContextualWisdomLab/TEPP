# Product and Technical Gap Baseline

**Status:** Active delivery recovery  
**Product:** Temporal Event Psychometrics Platform (TEPP)  
**Snapshot:** 2026-09-06T16:08:00Z  
**Protected-main evidence:** `a243f18da4a4ca8a8d068c39922537f1f8ed6ad0`  
**Workspace version:** `0.2.0`

**Delivery authority:** issue [#175](https://github.com/ContextualWisdomLab/TEPP/issues/175), PR [#435](https://github.com/ContextualWisdomLab/TEPP/pull/435), and [`docs/delivery/pr-queue-authority-2026-09-01.md`](delivery/pr-queue-authority-2026-09-01.md). Historical exact source/test/fixture/contract/ADR evidence remains in repository ancestry, `docs/research/`, `docs/TRACEABILITY.md`, and surviving landing vehicles. This file is the current operator register rather than a duplicate of every predecessor narrative.

## Delivery truth

A planning document, mergeable branch, local/source inspection, predecessor-head result, queued/skipped check, ADR number, bot status, or LLM judgment does not make a capability shipped. Only protected-main integration plus current required evidence establishes delivery.

| Signal | Fresh evidence | Delivery implication |
| --- | ---: | --- |
| Protected `main` | `a243f18da4a4ca8a8d068c39922537f1f8ed6ad0` | #490 remains integrated; product claims are bounded to this protected head until main advances. |
| Open pull requests | **135** | WIP circuit breaker remains active; consolidate into existing bounded-context vehicles. |
| Draft pull requests | **134** | #492 is Ready for Review; every other open PR remains Draft. |
| Non-Draft pull requests | **1** | #492 remains gated by canonical CodeQL machine-principal authorization and a qualifying independent approval. |
| Open issues | **17** | #491 owns systemic Validation Evidence exact-proof budgeting. |
| TEPP GitHub releases | **0** | No open TEPP head is a released contract. |
| Organization ruleset | `18156473` | Approval, stale-review dismissal, thread resolution, central required workflows, and non-fast-forward/deletion prohibitions remain normal delivery gates. |

Passing, queued, skipped, or predecessor-head checks on an open PR never promote that PR to implemented-main. Organization-admin bypass is not normal delivery evidence.

## Current priority evidence

#435 intentionally omits its own mutable branch SHA from this file.

| PR / issue | Exact current evidence | Disposition |
| --- | --- | --- |
| #492 | `794ba9e6dda9f043aa499920fdf609b81b075d7e`, Ready for Review, base `main` | Canonical foundation repair for the stale repository-local cron contract exposed after #490 centralized hourly admission. Both CodeRabbit findings are repaired. Rust Foundation `34011189084`, Documentation Quality, Security Scan, and Semgrep are exact-head GREEN. CodeQL `34011189081` is terminal failure before scan execution because canonical `.github` metadata binding sees `opencode-agent[bot]` while the owner allowlist remains `github-actions[bot]`; authorizing that app principal is an owner security decision. Fresh review submissions are COMMENTED only, with no qualifying `APPROVED`. |
| #488 | `eae187fcfc98e77a126907a06c7917fef90a5e2f`, Draft, base `main` | Validation Evidence landing vehicle. Production proof order is `neutral_zero_linear -> pairwise_reference -> generic_fallback`; exact sample admission remains `n=4..=16`. Repair `43d208383b765a3424019de45530af77ca484d78` removes a source-copy equivalence oracle; `eae187fc...` extends the independent `i128/u128` pair-distance oracle and forward/reversed/rotated bit identity to every admitted count `n=4..=16`. Current runs Rust Foundation `34044332463`, Documentation `34044332495`, Security `34044332501`, Semgrep `34044332462`, and CodeQL `34044332500` are non-terminal. No exact-head GREEN or qualifying review exists. |
| #491 | open | Replace the sample-count staircase with a measured exact-proof budget. Independent admitted-domain oracle breadth now covers every `n=4..=16`, but wider `n>16` Wide256/midpoint arithmetic remains characterization-only. Before a production budget change, require same-head GREEN, truthful route telemetry, exact-rounding invariants, Rust 1.98.0 release-mode timing and allocator/RSS evidence, and code-current TRACEABILITY. |
| #416 | `aa730c63563eb4a33048d822b581036c8487bd47`, Draft | Validation / Analysis Run landing vehicle. Generic cutoff-before-identity repair remains inherited; fold children keep unique source/tests/doctoring until conflict-resolving inheritance is verified. |
| #310 | `c6680450152b1e0a2c9abb553772d74a23923335`, Draft | Longitudinal Modeling vehicle. Stable `Between` recovery is unit-level with canonical occasion `0`; `Within` retains actual occasion identity. Older base remains a non-destructive restack finding. |
| #480 | `5b2637f7b6142573ba1385a652c56632ecc9faa5`, Draft | contextual-orchestrator released-contract fail-closed integration vehicle. Mutable owner main is evidence only until an immutable release exists. |

Exact-head evidence becomes stale after any source push.

## Domain ownership

TEPP owns Temporal Semantics, Event Ontology, Temporal Graph composition, irregular event time, time-varying multilevel/cross-classified/multiple-membership semantics, Longitudinal Modeling, leakage-safe knowledge cutoff, temporal recovery, Validation Evidence, and Projection policy. `longitudinal_core` is the current Rust implementation path for Longitudinal Modeling.

fast-mlsirm owns reusable static/generalized-mixed/dependence-aware psychometric specification and arithmetic. TEPP consumes only immutable released/versioned contracts through an ACL; source copying and mutable sibling-head dependencies are prohibited. Fresh owner evidence is fast-mlsirm protected `main@493326f2de49ea1704da0ded19868ed05d2fe00f` with latest immutable release `v0.9.1`.

contextual-orchestrator owns provider/model routing and semantic LLM execution. Fresh owner evidence is protected `main@414f22973658c4ddc3d4320fcf7acd9b4e8ba991` with no GitHub release, so TEPP must fail closed rather than consume mutable main as a production semantic-LLM contract. Cross-service SQL and source copying are prohibited. `AGENTS.md` now states the same immutable released-owner boundary and restricts model-backed Actions to the organization gateway `orchestrator/free` route.

The clock contract separates event/valid time, assertion time, document time, system time, availability time, and knowledge cutoff. Retrospective evidence may describe an earlier event but cannot enter an earlier knowledge cutoff. Forward state/transition edges remain distinct from retrospective/citation/revision/provenance relations.

## Scientific and implementation invariants

- Rasch remains distinct from generic 1PL; formulation-qualified 2PLM–5PLM, MIRT, ideal-point/GGUM, testlet/rater/facet/generalized-mixed identity is preserved.
- A nominal unit identifier is not repeated-measures evidence. Stable `Between` known truth is unit-level with canonical `occasion_index = 0`; `Within` retains actual `(unit, occasion)` identity.
- Row arrival order is not scientific evidence. Fixed admitted observations must produce bit-identical results under permutation wherever the deterministic CPU `f64` contract claims it.
- Mean signed bias and bias SE are Validation Evidence measures. `n=2` and proved `n=3` identities remain inherited from GAP-092–110. For production `n=4..=16`, the exact path first attempts neutral-zero signed-dyadic coordinates and exact Wide256 `P = nΣc_i²-(Σc_i)²`, then uses the O(n²) pairwise exact reference only as a fail-closed comparison route, then falls back to the established general implementation. Floating ratio/`sqrt` is only a candidate seed; exact denominator reduction, adjacent-midpoint comparison, and ties-to-even authorize the final binary64 result.
- The removed conditioned observed-anchor scan must not be reintroduced without a represented input proving unique bounded admission value that neutral-zero and pairwise cannot supply. No global theorem that pairwise is redundant is claimed.
- Pairwise-f64 exactness is not the represented-input admission definition. `[0,1,2^-54,2]`, `[0,1,2,-2^53]`, and `[1,2^-54,2,3]` show cases where a non-anchor binary64 subtraction can round while a represented-input exact route remains available.
- The mixed-sign witness `[-2^53,0,1,2^53]` is independently checked with integer pair-distance arithmetic: direct binary64 subtraction of `-2^53-1` loses the unit contribution, while the integer oracle retains `P=2^109+3`; the public route is pinned to `0x432a20bd700c2c3e`.
- Independent production-domain evidence now spans every admitted count `n=4..=16`: each represented family has a direct integer pair-square oracle and audited public binary64 result, and forward/reversed/rotated inputs must remain bit-identical. This is acceptance evidence, not proof of which internal route executed.
- The `n=16` cutoff is an implementation/resource boundary, not a scientific boundary. `n=17`, `2_047`, `4_095`, `208_064`, and wider represented fixtures are characterization points or arithmetic envelopes, not production budgets.
- Raw `D=2^58,n=65` refusal is invalid after common-unit normalization. Odd `D=2^58+1,n=65` remains an arithmetic narrow-width witness: exact pair numerator is 123 bits while narrow cancellation products require 129 bits.
- `081000289f5a52e94863026d55696ee2a4daf923` provides the dependency-free two-limb Wide256 characterization. `aab9fe9115cee97225f2aa81e54a55ceafb23336` demonstrates exponent-safe Wide256 exact comparison; RED `f7717361ad8c5f0592688c1514c104cc1b4adabe` → repair `e4a85f53a611922be7492fe906d62ce65787c18e` integrates signed-exponent comparison into the exact rounder. `1240ace8eb41a01fa72a4bb99df842fd550a1288` fixes both exact tie-to-even parity directions.
- `7a2ab0a1ef7a72d8cc9b9253d7f92c493e578943` supplies represented pair-versus-Wide256 exact-ratio equivalence where both routes admit. `2bc1d2284d75154e020640adb573c1cfadf005fb` proves pairwise-f64 admission is strictly narrower for `[0,1,2^-54,2]`. Repair `43d208383b765a3424019de45530af77ca484d78` removes copied production arithmetic from the integration oracle; `eae187fcfc98e77a126907a06c7917fef90a5e2f` broadens the independent admitted-domain evidence across all production sample counts.
- Historical-cutoff admission occurs before duplicate-identity checks. Future-unavailable evidence cannot change an earlier run's conflicts, counts, or terminal state.
- Supported temporal estimators require state/trajectory and claimed-structure recovery, bias/RMSE, interval coverage, convergence, uncertainty calibration, reproducibility, and leakage-safe rolling-origin evidence. Scientific failures are never hidden with skip/xfail/source rewriting/coverage exclusions.

## Current Validation Evidence repair and resource finding

#488 exact head `eae187fcfc98e77a126907a06c7917fef90a5e2f` inherits the GAP-045–125 lineage without widening the production sample budget.

The current production route is `neutral_zero_linear -> pairwise_reference -> generic_fallback`. Neutral-zero uses two represented-residual scans and exact signed-dyadic/Wide256 `nΣc_i²-(Σc_i)²`, giving O(n) proof time and O(1) proof storage after the residual vector. Pairwise remains O(n²) as the fail-closed comparison/reference path. The earlier O(n²) conditioned observed-anchor route was removed at `14e7862f4ddccce54f3b93d4dac89adbf047ba77` after no unique represented admission fixture justified keeping it.

Intermediate characterization `da703fc50e136ed79e12262909c9f400a3945621` copied production Wide256/dyadic/neutral-zero arithmetic into its own integration test. Repair `43d208383b765a3424019de45530af77ca484d78` removes that source-copy oracle. Test-only repair `eae187fcfc98e77a126907a06c7917fef90a5e2f` extends independent direct `i128/u128` pair-distance evidence and forward/reversed/rotated bit identity to every admitted sample count `n=4..=16`. Research doctoring `3ddba8db8ffa489d6f648a3ad3be766af6d92c96` and CHANGELOG doctoring `4521418dddabcf02d331b4581cc89e346d61641e` record the corrected source-copy boundary.

Wider Wide256 and exact-midpoint fixtures remain useful arithmetic characterization only. They do not establish production route acceptance beyond `n=16`. Resource arithmetic therefore stays separate from scientific admission: any cutoff change needs measured Rust 1.98.0 `--release` timing, allocator/RSS evidence, recorded hardware/build flags, and no sample shrinkage or omitted proof work.

Pinned predecessor Rustfmt Evidence `34037910108` produced artifact `9991468416`, digest `sha256:cd03e7c35be9d398d86494b01306df08af94dc117b38482578d652d9e2b2283b`, with 36 formatted Rust files plus patch/manifest. It is predecessor evidence only and is not same-head fmt GREEN for `eae187fc...`.

Current #488 exact-head runs are Rust Foundation `34044332463`, Documentation `34044332495`, Security `34044332501`, Semgrep `34044332462`, and CodeQL `34044332500`; all are non-terminal at this snapshot. Formal #488 reviews are empty. No current-head hosted GREEN, release-mode resource receipt, protected merge, release, or qualifying independent review is claimed.

Canonical `docs/TRACEABILITY.md` still needs a direct #491 neutral-zero/Wide256 production-resource row. That remains the checked-in documentation-authority debt for this Validation Evidence slice; this product/technical gap baseline is code-current as of the snapshot above.

## Gap register

| ID | Gap / state | Current closure evidence |
| --- | --- | --- |
| GAP-001–044 | Existing product/domain/DDD/release and Longitudinal gaps; mixed/inherited | Exact histories remain in ancestry, surviving landing vehicles, ADRs/tests, and research/TRACEABILITY. |
| GAP-045–091 | Validation Evidence bias, RMSE/SE, Monte Carlo, Wilson, matching, acceptance, provenance/serde/projection repairs; verification pending | Exact source/tests/research remain inherited on #488; protected-main integration still requires current-head gates/review. |
| GAP-092–110 | Represented residual subtraction, translated-moment, rational-scale, three-level and subnormal correctness sequence; verification pending | Exact RED→repair lineages and public contracts remain inherited on #488. Stale anchor-era test terminology has been consolidated away from production authority. |
| GAP-111–125 | Exact pair-distance/midpoint proof introduced and extended through `n=16`; verification pending | Exact-rounding primitives and Wide256 characterization remain inherited. Production route is neutral-zero first, pairwise reference second, generic fallback last; sample admission remains `n=4..=16`. |
| #491 | Hard sample-count cutoff and exact-proof route are incomplete resource/admission contracts; open | Independent oracle coverage now spans every admitted `n=4..=16` at `eae187fc...`; research `3ddba8...` and CHANGELOG `452141...` remain inherited. Same-head GREEN/review, route telemetry, measured CPU/RSS, and canonical TRACEABILITY indexing remain. |
| #492 | Central hourly admission tests remained stale/incomplete after #490; verification pending | Repairs through `794ba9e6...` remove the cron assertion and structurally validate `on:`. Rust/Docs/Security/Semgrep are exact-head GREEN; CodeQL remains blocked by canonical owner machine-principal authorization and qualifying approval remains absent. |

## Release gate

TEPP currently has no GitHub release. Release is permitted only after a coherent vertical reaches protected main with exact protected-head CI/security evidence, scientific/recovery acceptance, reproducible package/build artifacts with SBOM and provenance, validated migrations/upgrade/rollback/recovery where applicable, consistent version metadata and current CHANGELOG/TRACEABILITY/operator baseline, accessibility/operability evidence for user-facing components, no unresolved scientific/privacy/security/supply-chain blockers, and released integration contracts where deployment depends on them. Queued/pending/startup-failed/skipped or predecessor-head evidence is not GREEN.
