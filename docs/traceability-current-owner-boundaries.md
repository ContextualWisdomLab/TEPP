# Current numerical-owner and Validation Evidence traceability

**Status:** Draft traceability supplement
**Reviewed:** 2026-09-20
**Canonical documentation lane:** PR #435

This supplement records eleven exact-head obligations that are not yet represented directly in the legacy `docs/TRACEABILITY.md` matrix. It does not replace that matrix and does not convert mutable pull-request heads into release authority. The next safe matrix fold must preserve these rows verbatim in substance and then remove this supplement only after verified inheritance.

| Requirement / decision | Canonical basis | Exact source/evidence boundary | Maturity |
|---|---|---|---|
| Validation Evidence bias-SE resource/cardinality admission is distinct from numerical sample cardinality | PRD; Test Strategy; issue #491; PR #488 | `TEPP#488@520df488fd86ba48008af8ee5a2e112b4587fc22`; `analysis_engine::MAX_EVIDENCE_UNITS = 100_000` is an Analysis Run admission bound, not proof that one evidence unit maps to one `validation_core::bias_standard_error` truth/recovered pair. No typed/versioned buyer-path contract currently proves that mapping. Exact authored-production evidence remains 12,344/12,346 lines and 4,450/4,452 branch outcomes; the two unresolved obligations are `bias.rs:68-69` helper false-zero/resource reachability and `bias.rs:591-592` general translated-dispersion floating-positivity proof. | active-PR / unresolved scientific-product proof |
| Reusable correctly-rounded finite binary64 mean remains fast-mlsirm-owned and TEPP may consume only an immutable released contract | ADR 0001; TEPP #310/#495/#596; fast-mlsirm #1717/#1816 | TEPP consumer RED remains `TEPP#310@ba10820e0d28cc33d1b91ef37f6f6d163b3d91e9`, mixed-sign half-ULP actual bits `5080060379673919488` vs correctly-rounded expected `5080060379673919487`. Protected fast-mlsirm authority is `main@99c228a8f50aa8f2a9b1f95f3cc3c3edace9e781`. Upstream prerequisite #1717 is `f4bb9f694422cf5c34c9ef09126e62b6698567c0`, open/Ready/mergeable, ahead 192 / behind 0 after ordinary-forward reconciliation onto that main; fresh CI `35450948471`, Security `35450948431`, CodeQL `35450948468`, ClusterFuzzLite `35450948476`, Semgrep `35450948484`, and CodeQL PR `35450948509` remain queued, so predecessor-head GREEN does not transfer. Numerical owner candidate #1816 remains `7b853c2e8766c0dd58c3b01064c0dbcad1500493`, open/Draft/mergeable, ahead 19 / behind 1 relative to current protected main and must not be restacked ahead of #1717. Candidate `fast_mlsirm.binary64_mean@1.0.0` remains mutable and non-consumable. Latest immutable owner release is still `v0.11.4`, immutable and published `2026-09-18T08:14:12Z`; it predates #1717/#1816. | current owner heads recorded / blocked on exact-head gates, review, normal landing and immutable releases |
| Longitudinal CWC historical replay admits availability before per-row snapshot/domain provenance | Analysis Run leakage invariant; ADR 0033 Proposed; issues #592/#593/#595; child PR #372; surviving vehicle #416 | #372 is stacked on exact #416 base `03f8de2ed0a0fb842d2022d411814e440df7cfb4`. Public #595 RED `0ea8f6573d8519762e3ef9038ec4c4892c8e3a8b` requires a cutoff-visible foreign-snapshot row to fail closed while a future-unavailable foreign-snapshot row leaves artifact and terminal result equal to baseline. Causal repair `07360220cf8e7018107b271dc8e6e2c2f49b0c58` moves `AvailableTime` admission ahead of snapshot and evidence-identity admission while preserving raw `MAX_EVIDENCE_UNITS`. Current child head is `TEPP#372@47b6a6c85c69983bd843d8bb6d19001cfd3629d8`, Draft/open/mergeable, ahead 49 / behind 0 with eleven changed files. The surviving tree preserves #592/#593/#595/#596/#597/#599/#600/#601; #602/#603/#604 are owner/ACL or design prerequisites without TEPP Analysis production repairs yet. #501 remains the separate true-parameter recovery-acceptance owner. | active child repair; exact-head evidence pending |
| Longitudinal CWC digest-bound results must be invariant to enumeration order of the same cutoff-visible evidence | issue #596; child PR #372; fast-mlsirm numerical owner #1816 | `psychometric_core::recover_cluster_mean_within_between_slopes` currently forms predictor/outcome cluster means with ordered naive `f64` sums. Public consumer RED `TEPP#372@2a05f4d38f5136d32ab0170249dd3c47cf95949e` survives on current #372 head `47b6a6c85c69983bd843d8bb6d19001cfd3629d8` as `longitudinal_cwc_permutation_contract.rs`: the same six evidence identities with cluster-1 order `[1e16, 1, -1e16]` versus `[1e16, -1e16, 1]` must produce bit-identical artifact and terminal result. TEPP must not add a local numerical kernel or sort estimator inputs; resolution is gated on an immutable release of the fast-mlsirm finite-mean owner after #1717/#1816 land, followed by released-contract consumption and fresh #501 recovery evidence. | public consumer RED / blocked on immutable numerical owner |
| CWC stack reconciliation must preserve Cargo registry integrity while adding the production psychometric dependency | issue #597; child PR #372; surviving vehicle #416; Rust reproducibility/release gate | Public build RED `TEPP#372@5a5a2201b341bdc8c62cc23f0a2f62fc4dbe9c36` intended only to synchronize `analysis_engine.dependencies += "psychometric_core"` in `Cargo.lock`, but also mutated inherited registry checksums for `adler2`, `scopeguard`, `tracing-attributes`, `untrusted`, and `wasi`, and removed the final newline. Ordinary-forward repair `456f0a30f4dcaae10bc01bf902e83f75154d3dfb` restores the exact #416 registry checksums and newline while preserving the one intended dependency entry. Current compare against `#416@03f8de2...` keeps `Cargo.lock` at one addition / zero deletions. This is source/history proof only until one unchanged head passes `cargo metadata --locked`, applicable Rust/package gates, and normal checksum verification without rewriting the lockfile. | repaired source / hosted locked verification pending |
| Longitudinal CWC terminal artifacts cannot certify an all-singleton cluster population as a successful within/between decomposition | issue #599; child PR #372; `psychometric_core::recover_cluster_mean_within_between_slopes` | Public RED `TEPP#372@91377deed6fec2907ba34a33dd8bc122f58f3aa5` pins `row_count = 2`, `cluster_count = 2`. Under the current one-cluster-per-row profile, equality means every cluster is a singleton, so cluster-mean centering yields zero within-cluster predictor deviations and the canonical estimator must fail with a singular within design. Minimal repair `5e61f7df1d8fc16c93ce75af05e92e2046988c94` changes only the artifact structural invariant to `cluster_count < row_count`; cleanup `c40d306327309dee20d988b5d6699e0c3e706925` preserves the minimal regression delta. This condition is necessary but not sufficient for nonsingularity and is not scientific acceptance. | RED→source repair complete / hosted exact-head verification pending |
| Longitudinal CWC digest-bound artifacts must commit to the exact cutoff-visible admitted evidence population | issue #600; ADR 0033 Proposed; child PR #372; Analysis Run provenance boundary | Public RED `da1a846901f54f8500a30ae4a4988289da170916` changes one cutoff-visible opaque evidence identity while preserving snapshot, cutoff, cluster assignment, predictor/outcome coordinates, availability, counts and recovered slopes; the result digest must change. Causal repair `625a3a4562dbdb3527e3377eabb9231fc374f1b9` adds executor-owned `admitted_evidence_sha256`, computed after availability/snapshot/duplicate admission and before scientific composition from a versioned domain-separated, count/length-delimited encoding of evidence ID, snapshot ID, cluster key, exact `f64` bit patterns and canonical availability time. `676173fac5568f6147d560233aca5c1b9203da8d` migrates artifact fixtures and validates canonical lowercase-64hex import; `d2159307ec8e289fb5c5850c09c05bd668c6429e` proves a benign row permutation leaves the full artifact/terminal result unchanged; `988679869a8241ec5a7f932ee9fd1bf3df77dd42` keeps the helper slice-bounded. Canonicalization applies only to provenance hashing, not numerical estimator order, so #596 remains RED. Current #372 head is `47b6a6c85c69983bd843d8bb6d19001cfd3629d8`. The digest is content/provenance identity, not generator attestation or #501 scientific acceptance. | RED→source repair complete / hosted exact-head verification pending |
| Longitudinal CWC artifact identity uses one canonical textual representation of the knowledge cutoff | issue #601; `temporal_core::KnowledgeCutoff`; child PR #372; digest-bound artifact contract | Public RED `a5a039e4001e01d1d089e14c6c548d6d5ea60842` proves that canonical `2026-08-01T00:00:00Z` and semantically equivalent `2026-08-01T09:00:00+09:00` represented the same instant while the latter was still accepted as a valid digest-bound artifact spelling. Minimal repair `6c06c0de11d565f09e4cdba48834165f4518e909` parses once through `KnowledgeCutoff` and requires the serialized artifact field to equal `KnowledgeCutoff::to_rfc3339()`. ADR currentization `fe7a2821b2504841dd7696935198defd93148d4e` and doctoring/current child head `47b6a6c85c69983bd843d8bb6d19001cfd3629d8` preserve the semantic request-cutoff equality / canonical artifact-text distinction while keeping ADR 0033 Proposed. The fix is limited to wire identity/canonicalization; it does not modify cutoff admission, CWC arithmetic, #596, the admitted-evidence digest, or #501 recovery acceptance. | RED→source repair complete / hosted exact-head verification pending |
| Longitudinal CWC predictor and outcome coordinates must each bind to released Measurement authority, including exact construct identity | issue #602; TEPP #277 owner boundary; Psychometrics Commons #448/#449; child PR #372 | TEPP #277 assigns immutable result snapshots to Psychometrics Commons. Protected `psychometrics-commons@e81441ce70c676992470afe2be469dd891ad3eb5` pins assessment/instrument/scoring/calibration/norm/output-schema/engine provenance and per-observation `construct_ref`; no immutable owner release currently contains this new coordinate contract. Owner issue #448 is implemented on Draft PR `psychometrics-commons#449@8ca24956ba5e2ebb5f9e3d32c5d905a2e3d13d1b`, open/Draft/mergeable with eight changed files. Owner-side REDs `ae478bdfc5c51dffeb9082e3ea87583a63c189e3` and `e20dd2fa4462bd1d95eea83ab880c9579d691550` require construct-bound participant-free identity and unique canonical decode→re-encode; resource-safety RED `68fb77342660c38016825bb58a0be9f98a4a1150` additionally enforces normative QA-PERF-03 at the released ACL boundary. Causal repair `d5996e45cea62cd65043b1b2bd3138d0fafaf959` publishes an explicit 8 KiB v1 canonical-wire ceiling, rejects oversized bytes before UTF-8 parsing/copying, and preflights owner projection size before cloning provenance; `600232b9d53c32109a8de16a1d340ec78b40fe4f` covers the typed `CanonicalPayloadTooLarge` operator surface. A temporary scoring-API rewrite was ordinary-forward restored in `770181fb2eee8d2aaac220b44a33f856a371c251`; current source intent still has no `scoring.rs` delta. Documentation lineage now includes proposed ADR 0022 (`86b3d57a...`, completed at `8ca24956...`), changelog `80db263...`, ADR-index collision avoidance `63913b1...`, and active-PR traceability supplement `be9e09e...`; canonical `docs/TRACEABILITY.md` and affected Measurement Governance wording still require ordinary-forward reconciliation before merge. Exact-head Runtime CI run `35431439856` and Security/SAST/CodeQL/SBOM/provenance runs remain queued, and #449 has no qualifying independent approval. One scoring contract can emit multiple constructs; eventual TEPP #602 must bind separate released predictor/outcome coordinate authorities and fail closed on missing/malformed/unsupported/mutable/unreleased authority. | owner RED→source/decision-record repair active / blocked on #449 hosted acceptance, canonical docs, independent review, protected-main merge, immutable release, then TEPP consumer RED→repair |
| Longitudinal CWC historical admission must derive source snapshot/content and availability from owner-issued Evidence receipts rather than caller-authored fields | issue #603; Evidence owner Draft #527; child PR #372; surviving Analysis Run vehicle #416 | At `TEPP#372@47b6a6c85c69983bd843d8bb6d19001cfd3629d8`, `LongitudinalClusterScore::new` still accepts caller-provided `evidence_id`, `snapshot_id`, coordinates, and `AvailableTime`; #595 orders those supplied fields leakage-safely and #600 commits them, but neither authenticates source issuance or prevents backdating of a caller-selected availability value. Issue #603 therefore requires an owner-issued receipt before numerical admission. Evidence Draft `TEPP#527@82d43a8d398863df3c2ec6facd858883f367f1b5`, open/Draft/mergeable, owns `SourceArtifact` / `SourceObservation` / `SourceAvailability` and `SourceSnapshotReceiptV1`: Evidence mints artifact/observation/availability/receipt identities, copies immutable source-content SHA-256 plus distinct `SystemTime`/`AvailableTime`, enforces `AvailableTime >= SystemTime`, and keeps arbitrary JSON as validated-but-untrusted wire rather than promoting it to owner state. Current-head Rust Foundation `35432603156`, Semgrep `35432603109`, CodeQL PR `35432603133`, and Security `35432603146` remain queued, and #527 remains stacked behind foundation #538 with no released authority. #603 must wait for protected/released Evidence authority before adding the public backdating/source-digest RED and ACL repair; future-only receipts must remain invisible to historical census, and source-lineage provenance must stay distinct from #600 numeric-evidence commitment and #602 Measurement authority. | owner receipt contract active / blocked on #527 foundation, exact-head acceptance, review, protected landing/release, then #603 public RED→ACL repair |
| Longitudinal CWC must not silently collapse time-varying cross-classified or multiple membership to one caller-selected cluster | PRD; ADR 0003; issues #604/#606/#607/#608/#609/#610/#611/#612/#613/#614/#616/#617/#618/#619/#620/#621/#622; Membership owner PR #605; persistence successor #615; fast-mlsirm #2046/#2047/#2051/#2053/#2054/#2055; child PR #372; recovery issue #501 | Protected `main@a243f18da4a4ca8a8d068c39922537f1f8ed6ad0` defines `MembershipAssignment` as the atomic time-varying weighted membership unit. Current `TEPP#372@47b6a6c85c69983bd843d8bb6d19001cfd3629d8` still narrows every scientific row to one caller-supplied `cluster_key: u64`, so #604 remains the downstream Membership/Longitudinal ACL gap. Owner Draft `TEPP#605@c7defa251761f104e51a614cbba14f9f952bec58`, open/Draft/mergeable with 33 commits and 13 changed files, preserves #606 disjoint same-identity re-entry, #607/#613 finite positive `(0,1]` weight + serde contract, #608 partial-membership nested refusal, #609 mixed cross-classified + multiple membership, #610 exact-unity same-role composition, #611 population-level role heterogeneity, and #612 exact represented-binary64 share-budget admission. Exact-head Rust Foundation `35465243187`, Security `35465243143`, Semgrep `35465243169`, and CodeQL PR `35465243147` remain queued and no qualifying current-head approval exists. Persistence parity is a separate dependent lane: `TEPP#615@10b07fcdf47f867e3c7b78ba1b8e8a2a668cc4b3`, Draft/open/mergeable on #287, owns #614/#616–#622 row bounds, exact binary64 pointwise composition, PostgreSQL range semantics, predecessor validation, duplicate-edge parity and retry-safe concurrent admission; its exact-head Rust Foundation `35463797907` is queued. A separate DDD defect remains inside #605: `membership_core::icc::anova_nested_icc` contains reusable one-way random-intercept variance-component/ICC arithmetic. Canonical numerical-owner candidate is `fast-mlsirm#2047@1488b86c565eb14feab8464a27fbe97315c6fc2d` on protected `main@99c228a8...`. #2053 repairs bias acceptance and failure-denominator accounting; #2054 makes ICC/between/within recovery separate; #2055 RED `1cc0db4fb25e3d8286782367a7d42223cbeb07e5` proves a point ICC RMSE below `0.08` can still have `RMSE + 3*MCSE` above the declared threshold. #2055 repair `1488b86c565eb14feab8464a27fbe97315c6fc2d` computes RMSE MCSE from replication-level squared errors, requires the conservative `RMSE + 3*MCSE` bound for ICC and component thresholds, and expands the deterministic simulation denominator from 64 to 256 replications rather than weakening the targets. The DGP, seed stream, 120 unequal-size clusters per replication, production arithmetic, complete-recovery denominator, and bias rule remain unchanged. Fresh CI `35474954721` and fuzz `35474954732` are pending; CodeQL `35474954749`, CodeQL PR `35474954759`, Semgrep `35474954766`, and Security `35474954756` are queued, with no exact-head GREEN or qualifying independent approval. TEPP must not copy or depend on that mutable head. After normal owner landing and immutable release, #605 or its verified successor must remove duplicated ICC arithmetic, then #604 must add the Analysis RED/ACL that derives the v1 group mapping from admitted owner `GroupId`, preserves role/weight/event-time provenance and rejects unsupported cross-classified/multiple/heterogeneous structure. A future MMMC estimand requires its own versioned weighting rule, failure denominator and #501 recovery; row duplication remains forbidden. | owner/persistence/numerical repairs active / blocked on exact-head gates, reviews, protected landing, immutable numerical release, then #604 Analysis ACL and #501 recovery |

## Required owner sequence

The only accepted consumption sequence for the binary64 mean owner is:

`fast-mlsirm #1717@f4bb9f694422cf5c34c9ef09126e62b6698567c0 exact-head hosted acceptance + qualifying independent review` → normal protected-main merge → immutable fast-mlsirm release containing the landed prerequisite → `#1816` ordinary-forward reconciliation onto that protected result → exact-head numerical/oracle acceptance + qualifying independent review → normal merge → new immutable fast-mlsirm release with version/CHANGELOG/tag/package, SBOM/provenance, reproducibility, rollback, and numerical recovery evidence → TEPP released-contract bumps for #310 and the #596 CWC cluster-mean consumer → the mixed-sign and permutation REDs turn GREEN without copying upstream arithmetic or changing either estimand.

`v0.11.4` is release authority only for content actually present in that release. It is not evidence for #1717 post-restack/API-inventory repairs, #1816's binary64 mean candidate, or #2047's ICC candidate.

For Measurement provenance, the accepted owner path is:

`psychometrics-commons #448` → `#449` owner REDs and typed participant/source-text-free coordinate-provenance contract → bounded v1 wire and canonical decode→re-encode → Proposed ADR 0022 plus code-current canonical Measurement Governance/TRACEABILITY/change history → unchanged exact-head CI/security/privacy/coverage/review → normal protected-main merge → immutable Psychometrics Commons release with tag/package digest, SBOM/provenance, reproducibility and rollback/compatibility evidence → TEPP #602 consumes that released contract through an ACL and binds separate predictor/outcome coordinate authorities into CWC provenance → exact-head consumer GREEN without treating the pointer/fingerprint as construct-validity evidence.

For Evidence provenance, the accepted owner path is:

`TEPP #527` Evidence-owned source artifact/observation/availability/snapshot-receipt contract on the surviving foundation ancestry → unchanged exact-head Rust/security/SAST/CodeQL/coverage evidence + qualifying independent review → normal protected-main landing and immutable compatible TEPP release/owner contract → #603 public backdating/source-content-drift RED at the Analysis/Evidence ACL → Analysis consumes the owner receipt before numeric admission, uses the receipt `AvailableTime` as the historical cutoff clock, and binds its opaque receipt/source-content provenance into execution identity → exact-head consumer GREEN without treating a digest or syntactically validated wire object as owner authentication.

For Membership composition, the accepted owner path is:

`membership_core` protected-main time-varying weighted assignment authority → #605 owner RED/source repairs #604/#606–#613 → exact-head Rust/rustdoc/tests/coverage/security/CodeQL/Semgrep + qualifying independent review → normal protected-main landing of the design/admission boundary only after reusable local ICC arithmetic is removed or otherwise prevented from becoming wrong-owner protected-main truth → fast-mlsirm #2047 exact-head scientific/numerical acceptance including #2053 bias/failure-denominator evidence, #2054 separate between/within/ICC true-parameter recovery, and #2055 RMSE Monte Carlo-uncertainty evidence, qualifying independent review → normal fast-mlsirm merge and immutable released ICC contract → TEPP consumes that released contract through an ACL while retaining Membership design admission → #287 foundation/persistence owner lands, then #615 ordinary-forward restacks and proves PostgreSQL #614/#616–#622 parity before normal landing → #604 public structural-admission RED at the Membership/Longitudinal ACL → derive the v1 numerical group index only from admitted owner `GroupId`, preserve owner membership identity/role/weight/event-time validity, and fail closed when authoritative simultaneous/non-nested/heterogeneous memberships exceed the v1 estimand → reacquire #501 true-parameter recovery for the explicitly supported design.

For longitudinal CWC, #372 may close only after #416 or a verified successor inherits #592/#593/#595/#596/#597/#599/#600/#601/#602/#603/#604 source, tests, ADR/doctoring semantics, lockfile-integrity evidence, impossible-artifact refusal, admitted-evidence commitment, canonical cutoff identity, released Measurement authority, released Evidence source/clock authority, membership-structure admission authority, and the released numerical-owner migration while #435 preserves shared TRACEABILITY/ADR-index/product-gap history; fresh exact-head locked Rust/package, documentation/security/CodeQL, authored line/branch/edge evidence, qualifying review, and #501 scientific acceptance (or equivalent checked-in evidence) must then be reacquired on the surviving authority.

## Resource/cardinality decision record

The `100_000` Analysis Run evidence-unit ceiling must not be copied into `validation_core` or treated as a public numerical-domain cutoff. Before a finite product-side metric-population bound can discharge the remaining bias-SE floating proof, the supported buyer path must state and enforce how admitted evidence becomes truth/recovered metric pairs. Until then, the source-domain `n = 2^54` false-zero witness remains valid even though it is resource-extreme, and the two authored coverage obligations remain real rather than suppressible.

## Promotion boundary

This document is traceability evidence, not acceptance evidence. It authorizes no self-approval, gate weakening, mutable-owner consumption, foreign-owner source copy, coverage denominator reduction, skip/xfail, or predecessor-head receipt transfer. `docs/TRACEABILITY.md` remains the canonical matrix and must absorb these eleven rows ordinary-forward in PR #435 before the documentation lane can claim fully code-current traceability.
