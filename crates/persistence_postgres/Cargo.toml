[package]
name = "persistence_postgres"
description = "PostgreSQL adapters for bitemporal TEPP domain contracts."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
publish = false

[features]
default = []
## Enable compiled `SQLx`/`PostgreSQL` pool construction and query transport.
live-sqlx = ["dep:sqlx", "dep:tokio"]

[dependencies]
operational_log = { path = "../operational_log", version = "0.2.0" }
temporal_core = { path = "../temporal_core", version = "0.2.0" }
tepp_api = { path = "../tepp_api", version = "0.2.0" }
sha2.workspace = true
uuid.workspace = true
sqlx = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }

[dev-dependencies]
analysis_engine = { path = "../analysis_engine", version = "0.2.0" }
uuid.workspace = true

[lints]
workspace = true
